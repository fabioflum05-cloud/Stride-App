# Stride-App

## Permissions

The user has given standing permission to run all commands without asking for confirmation first, including:
- `npm install`, `npm uninstall`, `npx expo install`
- `eas build`, `eas submit` (including `--auto-submit` to TestFlight)
- Any other build/dependency-management commands

Do not pause to ask before running these.
